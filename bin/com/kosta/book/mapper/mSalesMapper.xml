<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kosta.book.admin.mSales.model.SalesDAO">

	<select id="saleNewBook" resultType="com.kosta.book.admin.mSales.model.SalesListVO">
<<<<<<< HEAD
		select * from salelist where branchName like '${branchName}' and isbn in (select isbn from book_info where type='국내도서' or type='외국도서')
	</select>
	
	<select id="saleUsedBook" resultType="com.kosta.book.admin.mSales.model.SalesListVO">
		select * from salelist where branchName like '${branchName}' and isbn in (select isbn from book_info where type='중고')
	</select>
	
	<select id="sumSaleNewBook" resultType="String">
		select sum(quantity * dprice) from salelist where branchName like '${branchName}' and isbn in (select isbn from book_info where type='국내도서' or type='외국도서')
	</select>
	
	<select id="sumSaleUsedBook" resultType="String">
		select sum(quantity * dprice) from salelist where branchName like '${branchName}' and isbn in (select isbn from book_info where type='중고')
=======
		select * from salelist where branchName like '${branchName}' and isbn in (select isbn from book_info where type='국내도서')
	</select>
	
	<select id="saleForeignBook" resultType="com.kosta.book.admin.mSales.model.SalesListVO">
		select * from salelist where branchName like '${branchName}' and isbn in (select isbn from book_info where type='외국도서')
	</select>

	<select id="sumSaleDomesticBook" resultType="String">
		select nvl(sum(dprice * quantity), 0) from salelist where branchname=#{ branchName } and isbn in (select isbn from book_info where type='국내도서')
	</select>
	
	<select id="sumSaleForeignBook" resultType="String">
		select nvl(sum(dprice * quantity), 0) from salelist where branchname=#{ branchName } and isbn in (select isbn from book_info where type='외국도서')
>>>>>>> branch 'newMaster' of https://github.com/ChoeSuJin/JangMoonBook.git
	</select>

</mapper>